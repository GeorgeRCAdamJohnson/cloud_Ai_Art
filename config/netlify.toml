[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--production=false"
  SKIP_PREFLIGHT_CHECK = "true"

# Essential plugin for Next.js - handles everything automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"